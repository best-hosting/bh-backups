---
# defaults file for bh-backups
install_path: '/etc'

bh_root_rsync_filter: |
  # BH / filter.

  . {{ rsync_filters_libdir }}/ossec.rsync-filter
  . {{ rsync_filters_libdir }}/ispmgr.rsync-filter

  . {{ rsync_filters_libdir }}/root-base.rsync-filter

bh_home_rsync_filter: |
  # BH filter for user's data.

  # Ignore web logs.
  - /httpd-logs/*
  - /*/data/logs/*

  . {{ rsync_filters_libdir }}/home-base.rsync-filter

r_backups:
  - name: 'root'
    src:  '/'
    dst:  'root'
    rsync_filter: "{{ bh_root_rsync_filter }}"
  - name: 'var_www'
    src:  '/var/www'
    dst:  'var/www'
    rsync_filter: "{{ bh_home_rsync_filter }}"
  - name: 'home'
    src:  '/home'
    dst:  'home'
    rsync_filter: "{{ bh_home_rsync_filter }}"

# name must be unique in single r_backup_scripts list!
r_backup_scripts:
  - name:   'mysql'
    script: "{{ role_path + '/files/mysql.sh' }}"
    password: 'suka-blyat'
  - name:   'mysql-her'
    script: "{{ role_path + '/files/mysql.sh' }}"
    password: 'blyaaaa-huuuy'
  - name:   'pgsql'
    script: "{{ role_path + '/files/pgsql.sh' }}"
    password: 'naher-epta'
  - name:   'pgsql-bzz'
    script: "{{ role_path + '/files/pgsql.sh' }}"
    password: 'epta-nahuy'



r_snapshot_root: '/var/cache/rsnapshot'

# Default database user name.
db_user: "{{ backup_server }}"

# Default database user password.
db_password: "{{ lookup('file', 'backup_mysql.password') }}"

